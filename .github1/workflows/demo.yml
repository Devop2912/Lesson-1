name: hello world 

    on :
      push :
         branch: [main]


    jobs:
        demo: 
           run-on: ubuntu-latest

           steps:
              name: Greetings;
               run: "hello, world"
